# NammaArasu Staging Configuration (Forced Browser Sandboxed Storage Mode)
# This forces the staging build to run in local-first browser storage mode.
# No Firebase API credentials or separate cloud setup are required, ensuring 100% data isolation from production!
NEXT_PUBLIC_FORCE_LOCAL=true
